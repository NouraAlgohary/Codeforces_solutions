#include <bits/stdc++.h>
#include<cstdio>
#include<cmath>
using namespace std;
int main()
{
	long  n;
	while (scanf("%ld",&n) && n != 0)
	{
		long  root;
		root = sqrt(n);

		if (root*root!=n)
			cout << "no" << endl;
		else
			cout << "yes" << endl;
	}
	return 0;
}
